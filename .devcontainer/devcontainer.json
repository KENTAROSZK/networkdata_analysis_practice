{
  "name": "project_template (compose)",
  "dockerComposeFile": [
    "../context/docker-compose.yaml"
  ],
  "service": "dev",
  "workspaceFolder": "/workspace",
  "overrideCommand": false,
  "remoteUser": "${localEnv:USER_NAME}"
}
